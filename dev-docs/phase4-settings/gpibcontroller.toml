# GpibController Settings Review
# Status: pending
#
# Implementations:
#   VirtualGpibController, PrologixGpibLAN, PrologixGpibUSB, PythonGpibController
#
# Priority: Required | Important | Optional
#
# NOTES:
#   1. This hardware type has NO registrable settings:
#        VirtualGpibController  -- no setDefault calls at all
#        PrologixGpibLAN/USB    -- only comm settings (timeout, termChar, commType),
#                                  all excluded from registration
#        PythonGpibController   -- configParams() returns {} (empty)
#
#   2. Migration work for this type is purely mechanical:
#        - Add REGISTER_HARDWARE_SETTINGS with no entries (empty macro call is valid)
#        - Remove REGISTER_HARDWARE_PARAMS and configParams() from PythonGpibController
#        - No constructor changes needed
#
#   3. No TOML review table is needed. This file is a placeholder to document
#      that the type was reviewed and confirmed as having no settings.
# ==============================================================================

# No settings to register for any GpibController implementation.
# Implementation-specific notes are in the NOTES section above.
